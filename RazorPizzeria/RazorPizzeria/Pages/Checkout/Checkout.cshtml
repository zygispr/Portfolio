@page
@model RazorPizzeria.Pages.Checkout.CheckoutModel
@{
    ViewData["Title"] = "Checkout";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<div class="text-center">
    <div class="mainText">
        <h1 class="display-4 text-black">Your order</h1>
    </div>
    
    @{
        var imagePath = $"~/images/Pizzas/{@Model.ImageTitle}.png";
    }
    
    <div class="align-content-center">
        <div class="col-lg-4 col-md-6 col-sm-6 mx-auto mb-5">
            <img src="@imagePath" class="w-100 rounded-3" asp-append-version="true"/>
            <p>Your order for the @Model.PizzaName pizza is ready</p>
            <p>Total price for your order is: @Model.PizzaPrice€</p>
            <a class="btn btn-primary mt-2 mx-auto" asp-area="" asp-page="ThankYou">Confirm order</a>
        </div>
    </div>
</div>